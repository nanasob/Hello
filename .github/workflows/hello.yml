name: Hello
on: 
  push:
  workflow_dispatch:

jobs:
    Deploy:
    needs: Test
    runs-on: ubuntu-latest
    steps:
      - name: Deploy code
        run: |
          echo "Deploying code"
          echo "Deployment Successful"